[package]
name = "lem-protocol"
description = "Types and codec for the lem-server JSON-RPC display protocol."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
